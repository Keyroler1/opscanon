name: RepoHandoff

on:
  pull_request:

jobs:
  repohandoff:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          path: .
          out: repohandoff-artifacts
          comment: "false"
