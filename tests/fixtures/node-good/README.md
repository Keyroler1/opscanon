# Node Good

This package exposes a CLI and an MCP server for AI agents.

## Setup

```bash
npm install
npm run build
```

## Usage

```bash
node dist/index.js scan ./repo
```

## Agent usage

Agents can call the CLI with JSON output. Authentication uses environment variables.
